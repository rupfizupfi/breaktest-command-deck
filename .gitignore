*/build/
/build/
/target/
*/target/
/.data/
*/.data/
.idea/
.vscode/
.gradle/
.settings
.project
.classpathnpm

*.iml
.DS_Store

node_modules/
*/src/main/frontend/generated/
# Vaadin production frontend bundle (regenerable, ~38 MB)
*/src/main/bundles/
pnpmfile.js
vite.generated.ts

# Browser drivers for local integration tests (TestBench downloads them to the repo root).
# Anchored: unanchored, it also swallowed command-deck/src/drivers/, the optional vendor-adapter
# source set, whose files then never showed up in git status at all.
/drivers/
# Error screenshots generated by TestBench for failed integration tests
error-screenshots/
webpack.generated.js

frontend/themes/hilla-react-app/theme-editor.css

.fleet/

lib/usbmodbus.jar
script/.data
scrtip/app.log

/.secrets
# Local-only deployment secrets: PKCS12 keystore holds an SSL private key
docker/keystore/
docker/.secrets/
# Database dump taken from a live instance
docker/backup.sql

# Runtime settings written by SettingRepository in dev
/settings.json
*/settings.json
# ...but the Claude Code hook wiring is real config, not a runtime artifact
!.claude/settings.json

*.bak

# Classes extracted from lib/usbmodbus.jar during investigation. That jar's
# licence forbids redistribution, so its decompiled contents must not be
# committed either.
umb/

# supervisord logs and perms dumps from container runs
docker/test/